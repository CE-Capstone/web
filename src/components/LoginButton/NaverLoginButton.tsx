import LoginButton from "./LoginButton";

function NaverLoginButton() {
  return (
    <LoginButton
      src="https://logo.clearbit.com/Naver.com?size=500"
      text="네이버 아이디로 로그인"
    />
  );
}

export default NaverLoginButton;
